{
  "buildCommand": "npm --prefix remtodo-frontend install && npm --prefix remtodo-frontend run build",
  "outputDirectory": "remtodo-frontend/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
